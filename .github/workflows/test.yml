name: Test

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Run ctest
        run: |
          echo $GITHUB_WORKSPACE
          ls -d $GITHUB_WORKSPACE
          cd $GITHUB_WORKSPACE/build
          ctest
